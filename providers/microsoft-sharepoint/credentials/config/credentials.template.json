{
  "client_id": "CLIENT_ID",
  "client_secret": "CLIENT_SECRET",

  "protocol": "https",

  "authorization_url": "https://login.microsoftonline.com/common/oauth2/v2.0/authorize",
  "token_url": "https://login.microsoftonline.com/common/oauth2/v2.0/token",

  "auth_hostname": "login.microsoftonline.com",
  "auth_version": "v2.0",

  "auth_url": "${PROTOCOL}://${AUTH_HOSTNAME}/common/oauth2/${AUTH_VERSION}/authorize",
  "auth_token_url": "${PROTOCOL}://${AUTH_HOSTNAME}/common/oauth2/${AUTH_VERSION}/token",
  "auth_user_url": "${PROTOCOL}://graph.microsoft.com/v1.0/me/",

  "auth_scope": "Files.ReadWrite.All Sites.Manage.All Sites.ReadWrite.All User.Read User.ReadBasic.All profile email openid offline_access",

  "api_hostname": "graph.microsoft.com",
  "api_version": "v1.0",
  "api_base_url": "${PROTOCOL}://${API_HOSTNAME}/${API_VERSION}"
}
